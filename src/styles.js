import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  appBar: {
    display: 'flex',
    justifyContent: 'center',
    aliginItems: 'center',
    flexDirection: 'row',
    borderRadius: 15
  }
}))